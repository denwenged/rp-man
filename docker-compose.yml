services:
  rp-man:
    build:
      context: .
      dockerfile: Dockerfile
    image: rp-man:latest
    container_name: rp-man
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATA_DIR=/app/data
      - OLLAMA_HOST=http://host.docker.internal:11434
      # Optional Discord Bot token (can also be configured via Web Control Panel)
      - DISCORD_BOT_TOKEN=
      - JWT_SECRET=change-this-to-a-secure-random-secret
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    extra_hosts:
      # Allows container on ZimaOS / Linux / macOS to reach the host's Ollama instance
      - "host.docker.internal:host-gateway"
    # Resource constraints tailored for low-power servers (e.g. ZimaOS, CasaOS, N100 mini-PCs)
    deploy:
      resources:
        limits:
          memory: 1024M

  # Optional Ollama container (uncomment if you want Ollama managed inside the same compose)
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: rp-man-ollama
  #   restart: unless-stopped
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ./ollama_data:/root/.ollama
  #   # For NVIDIA GPU passthrough on Linux/ZimaOS:
  #   # deploy:
  #   #   resources:
  #   #     reservations:
  #   #       devices:
  #   #         - driver: nvidia
  #   #           count: all
  #   #           capabilities: [gpu]
